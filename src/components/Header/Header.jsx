import { Space } from "antd"

export const Header = () => {
  return (<>
    <div
      style={{
        height: 60,
        display: "flex",
        alignItems: "center",
        fontWeight: "bold"
      }}
    > 
      <Space>
      HEADER
      </Space>
    </div>
  </>)
}
